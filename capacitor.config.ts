import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'SmartTank',
  webDir: 'dist/app-microprocesadores',
  bundledWebRuntime: false
};

export default config;
