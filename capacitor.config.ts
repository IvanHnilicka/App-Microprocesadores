import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'SmartTank',
  webDir: 'dist/SmartTank',
  bundledWebRuntime: false
};

export default config;
